# راهنمای اصلاحات (FIXES_README)

این پوشه شامل فایل‌های اصلاح‌شده برای ۵ باگ گزارش‌شده توسط کارفرما است.

## فایل‌های اصلاحی

| فایل | توضیح |
|------|-------|
| `lavayeh_scenario.py` | اصلاح باگ #۱+#۲+#۳ (ثبت وکالت‌نامه الکترونیک در لایحه) و باگ #۴ (شخص حقوقی نماینده) |
| `ezhharnameh_scenario.py` | اصلاح باگ #۱+#۲+#۳ (ثبت وکالت‌نامه در اظهارنامه) و باگ #۵ (نماینده شخص حقوقی) |
| `check_scenario.py` | اصلاح باگ #۱+#۲+#۳ (ثبت وکالت‌نامه در دادخواست) و باگ #۵ (اعسار و سایر عناوین) |
| `ealam_vakalaht_scenario.py` | اصلاح باگ #۱+#۲+#۳ (ثبت وکالت‌نامه در اعلام وکالت) |
| `tajdid_nazar_scenario.py` | اصلاح باگ #۱+#۲+#۳ (ثبت وکالت‌نامه در تجدیدنظر) و باگ #۵ (نماینده) |
| `handlers.py` | افزودن هندلر پیام قرارداد جدید برای ادامه ثبت وکالت‌نامه |
| `vakalaht_resume_handler.py` | **فایل جدید** — ماژول ادامه ثبت وکالت‌نامه با شماره قرارداد جدید |

## خلاصه اصلاحات

### باگ #۱ + #۲ + #۳: ثبت وکالت‌نامه الکترونیک و مدیریت پاپ‌آپ‌ها

**مشکل**: وقتی در بخش «منظمات» شماره قرارداد وکالت و مبلغ حق‌الوکاله وارد
می‌شد و `#btnSaveDoc` کلیک می‌شد، کد قبلی فقط پاپ‌آپ موفقیت را تشخیص می‌داد
و در سایر موارد ۳ بار retry می‌کرد که بی‌فایده بود.

**راه‌حل**: تابع `_upload_electronic_vakalaht` (و نسخه‌های مشابه در همهٔ
سناریوها) بازنویسی شد تا یک `status string` برگرداند:

- `"success"`: پاپ‌آپ موفقیت ظاهر شد — ثبت موفق بود.
- `"invalid_contract"`: پاپ‌آپ «شماره قرارداد الکترونیک وکالت ... معتبر نمی
  باشد» — این مرحله اسکیپ می‌شود، سایر پیوست‌ها انجام می‌شود، سپس به کاربر
  اعلام می‌شود که ۴۵ دقیقه فرصت دارد شماره قرارداد جدید ارسال کند.
- `"concurrent_login"`: پاپ‌آپ «ورود به سامانه در صفحه یا رایانه ای دیگر» —
  نشست منقضی شده، `handle_session_expired` صدا زده می‌شود و فرآیند متوقف
  می‌شود تا کارفرما لاگین مجدد انجام دهد.
- `"failed"`: خطای ناشناخته پس از ۳ تلاش.

تابع کمکی `_detect_vakalaht_popup` (یا `_detect_vakalaht_popup_check` در
check_scenario.py) برای تشخیص نوع پاپ‌آپ اضافه شد.

### باگ #۲ (بخش دوم): ادامه ثبت با قرارداد جدید

وقتی کاربر پس از دریافت پیام «شماره قرارداد نامعتبر است» شماره قرارداد
جدید (۱۶ رقمی) را ارسال می‌کند، ماژول جدید `vakalaht_resume_handler.py`
مراحل زیر را انجام می‌دهد:

1. یافتن تسک vakalaht معلق برای کاربر در `runtime_state.incomplete_tasks`
2. ریلود سامانه و استعلام با کدرهگیری ثبت‌شده (bill_no)
3. ورود به مرحله «منظمات»
4. فقط شماره قرارداد جدید را وارد می‌کند (بدون حق‌الوکاله اضافی — طبق
   دستور کارفرما)
5. کلیک `#btnSaveDoc` و بررسی پیام تأیید
6. ادامه مراحل: آماده‌سازی، محاسبه هزینه، چاپ
7. ارسال PDF و پیام نتیجه به کاربر

هندلر پیام در `handlers.py` اضافه شد: `handle_vakalaht_resume_message`
که قبل از سایر هندلرهای state-sensitive اجرا می‌شود.

### باگ #۴: شخص حقوقی نماینده در لایحه

**مشکل**: در `process_lavayeh_task` وقتی شخص حقوقی انتخاب می‌شد، پس از
تکمیل فیلدهای شرکت، کد قبلی مستقیماً روی رادیو باتن `value="7"` کلیک
می‌کرد بدون اینکه ابتدا دکمهٔ «افزودن» (`#btnAddSection`) را بزند. در
نتیجه فیلد `#txtRealIrNationalityCode` هرگز رندر نمی‌شد و `page.fill`
با Timeout 30000ms شکست می‌خورد.

**راه‌حل** (در `lavayeh_scenario.py`):
1. بعد از تکمیل شخص حقوقی، `_click_add_person` (`#btnAddSection`) کلیک
   می‌شود تا یک سکشن جدید برای نماینده باز شود.
2. سپس روی رادیو باتن `#rdb7` (value=7) کلیک می‌شود.
3. نوع نماینده از dropdown انتخاب می‌شود.
4. کدملی نماینده در `#txtRealIrNationalityCode` وارد می‌شود (با ۸ تلاش).
5. استعلام نماینده انجام می‌شود.
6. **طبق دستور کارفرما**: پیوست کردن مدرک نمایندگی در لایحه نیازی نیست.

### باگ #۵: اعسار و سایر عناوین دادخواست — نماینده اسکیپ می‌شود

**مشکل**: در `_fill_legal_person` (check_scenario.py) و توابع مشابه در
ezhharnameh_scenario.py و tajdid_nazar_scenario.py، حلقهٔ پر کردن
`#txtRealIrNationalityCode` فقط ۵ تلاش (۱۵ ثانیه) داشت و اگر فیلد disabled
بود (که در فرم‌های کند سامانه، به‌ویژه در عناوین اعسار، پیش می‌آمد) بی‌صدا
رد می‌شد. سپس چون `_collect_legal_reps` فقط `reps[1:]` را برمی‌گرداند،
اولین نماینده نیز هرگز در مرحلهٔ «نماينده» پردازش نمی‌شد — یعنی هیچ
نماینده‌ای ثبت نمی‌شد.

**راه‌حل**:
1. تعداد تلاش‌ها از ۵ به ۱۲ افزایش یافت.
2. سلکتورهای متعدد اضافه شد: `#txtRealIrNationalityCode, #txtRealIrNationalityCode1, input[ng-model$=".NationalityCode"]`.
3. شرط `inp.offsetParent !== null` اضافه شد تا فقط فیلدهای visible انتخاب شوند.
4.触发 events `input` و `change` و `$apply` به‌صورت صحیح AngularJS.
5. لاگ خطا وقتی فیلد پیدا نشد.
6. علامت‌گذاری `person["_rep_fill_failed"] = True` وقتی پر کردن شکست خورد.
7. `_collect_legal_reps` در check_scenario.py حالا اگر `_rep_fill_failed`
   باشد، اولین نماینده را هم در مرحلهٔ «نماينده» پردازش می‌کند (به‌عنوان
   fallback).
8. مرحلهٔ «نماينده» در check_scenario.py نیز همان اصلاحات را دارد (۱۲
   تلاش، سلکتورهای متعدد، لاگ خطا، اطلاع به مدیر).

## نحوهٔ استقرار

1. این فایل‌ها را در مسیر ریشهٔ پروژه جایگزین کنید:
   ```
   cp lavayeh_scenario.py /path/to/project/
   cp ezhharnameh_scenario.py /path/to/project/
   cp check_scenario.py /path/to/project/
   cp ealam_vakalaht_scenario.py /path/to/project/
   cp tajdid_nazar_scenario.py /path/to/project/
   cp handlers.py /path/to/project/
   cp vakalaht_resume_handler.py /path/to/project/  # فایل جدید
   ```
2. ربات را ری‌استارت کنید.

## نکات مهم

- **فایل جدید `vakalaht_resume_handler.py`** در کنار سایر فایل‌های پایتون
  قرار گیرد (همان مسیر `handlers.py`).
- وقتی کاربر قرارداد جدید می‌فرستد، ربات آن را در `runtime_state.incomplete_tasks`
  با کلیدهایی مثل `lavayeh_vakalaht:{bill_no}`, `ezhhar_vakalaht:{bill_no}`
  و... جستجو می‌کند. این تسک‌ها پس از ثبت موفق یا پس از ۴۵ دقیقه به‌صورت
  خودکار حذف می‌شوند.
- اگر کاربر در حال ثبت یک تسک دیگر (مثلاً در state FSM) است و قرارداد
  ۱۶ رقمی می‌فرستد، هندلر `handle_vakalaht_resume_message` فقط در صورتی
  پیام را هندل می‌کند که تسک vakalaht معلق برایش وجود داشته باشد. در غیر
  این صورت، پیام به هندلرهای بعدی می‌رود.

## ⚠️ نکتهٔ مهم دربارهٔ فیلتر هندلر قرارداد جدید

هندلر `handle_vakalaht_resume_message` با یک **فیلتر سفارشی** به‌نام
`_is_vakalaht_resume_candidate` ثبت شده است. این فیلتر فقط زمانی `True`
برمی‌گرداند که:
1. کاربر پیام متنی ۱۶ رقمی فرستاده باشد (دقیقاً ۱۶ رقم)
2. کاربر در `runtime_state.incomplete_tasks` یک تسک vakalaht معلق داشته باشد

در غیر این صورت propagation ادامه می‌یابد و سایر هندلرها (دکمه‌های کیبورد،
کدملی، کدرهگیری و...) به‌طور عادی کار می‌کنند.

**⚠️ هشدار**: اگر قبلاً نسخهٔ اول اصلاحیه را نصب کرده‌اید (با فیلتر `F.text`
بدون محدودیت)، آن نسخه باعث می‌شد تمام دکمه‌های کیبورد ربات کار نکنند.
حتماً این نسخهٔ جدید را جایگزین کنید.
