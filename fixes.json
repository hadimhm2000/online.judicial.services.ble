[
 {
  "file": "keyboards.py",
  "title": "فعال‌سازی-دکمه‌های چک و دعاوی اعتراضی در get_flow_type_kb",
  "old": "def get_flow_type_kb(user_id: int) -> ReplyKeyboardMarkup:\n    \"\"\"کیبورد منوی اصلی — دکمه تست فقط برای مدیر.\"\"\"\n    _is_admin = (user_id == ADMIN_ID)\n    _tn = \"⚖️ دعاوی اعتراضی\" if _is_admin else \"⚖️ دعاوی اعتراضی (به زودی)\"\n    _chk = \"🏦 ثبت دادخواست چک\" if _is_admin else \"🏦 ثبت دادخواست چک (به زودی)\"\n    rows = [",
  "new": "def get_flow_type_kb(user_id: int) -> ReplyKeyboardMarkup:\n    \"\"\"کیبورد منوی اصلی — دکمه تست فقط برای مدیر.\n\n    ⭐ اصلاحیه: «دعاوی اعتراضی» و «ثبت دادخواست چک» برای همهٔ کاربران\n    فعال شدند — برچسب «(به زودی)» حذف گردید.\n    \"\"\"\n    _is_admin = (user_id == ADMIN_ID)\n    _tn = \"⚖️ دعاوی اعتراضی\"\n    _chk = \"🏦 ثبت دادخواست چک\"\n    rows = ["
 },
 {
  "file": "keyboards.py",
  "title": "فعال‌سازی-دکمه‌ها در flow_type_kb (fallback)",
  "old": "        [KeyboardButton(text=\"⚖️ دعاوی اعتراضی (به زودی)\"), KeyboardButton(text=\"🏦 ثبت دادخواست چک (به زودی)\")],",
  "new": "        [KeyboardButton(text=\"⚖️ دعاوی اعتراضی\"), KeyboardButton(text=\"🏦 ثبت دادخواست چک\")],"
 },
 {
  "file": "handlers.py",
  "title": "فعال‌سازی-حذف چک مدیر از ورود دعاوی اعتراضی",
  "old": "    elif \"دعاوی اعتراضی\" in message.text:\n        if message.from_user.id != ADMIN_ID:\n            await message.answer(\n                \"⏳ *بخش دعاوی اعتراضی در حال توسعه است.*\\n\\n\"\n                \"این سرویس به‌زودی برای استفاده عمومی فعال خواهد شد.\\n\"\n                \"برای پیگیری اخبار به کانال ما مراجعه کنید:\\n\"\n                \"https://ble.ir/onlinejudicialservice\",\n                reply_markup=get_main_menu_kb(message.from_user.id))\n            return\n        from tajdid_nazar_handlers import tajdid_nazar_entry\n        await tajdid_nazar_entry(message, state)",
  "new": "    elif \"دعاوی اعتراضی\" in message.text:\n        # ⭐ اصلاحیه: این بخش برای همهٔ کاربران فعال است (قبلاً فقط مدیر\n        # می‌توانست وارد شود و بقیه پیام «در حال توسعه» دریافت می‌کردند).\n        from tajdid_nazar_handlers import tajdid_nazar_entry\n        await tajdid_nazar_entry(message, state)"
 },
 {
  "file": "handlers.py",
  "title": "فعال‌سازی-حذف چک مدیر از ورود ثبت دادخواست چک",
  "old": "    elif \"دادخواست چک\" in message.text:\n        if message.from_user.id != ADMIN_ID:\n            await message.answer(\n                \"⏳ *بخش ثبت دادخواست چک در حال توسعه است.*\\n\\n\"\n                \"این سرویس به‌زودی برای استفاده عمومی فعال خواهد شد.\\n\"\n                \"برای پیگیری اخبار به کانال ما مراجعه کنید:\\n\"\n                \"https://ble.ir/onlinejudicialservice\",\n                reply_markup=get_main_menu_kb(message.from_user.id))\n            return\n        from check_handlers import check_entry\n        await check_entry(message, state)",
  "new": "    elif \"دادخواست چک\" in message.text:\n        # ⭐ اصلاحیه: این بخش برای همهٔ کاربران فعال است (قبلاً فقط مدیر\n        # می‌توانست وارد شود و بقیه پیام «در حال توسعه» دریافت می‌کردند).\n        from check_handlers import check_entry\n        await check_entry(message, state)"
 }
]