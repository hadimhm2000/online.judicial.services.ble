# راهنمای نصب ماژول استعلام ارزش منطقه‌ای

## نحوه استفاده

این پوشه شامل فایل‌های جدید و یک اسکریپت خودکار (apply_patch.py) است.

### مرحله ۱: کپی فایل‌ها به پروژه
پوشه فعلی را در کنار فایل‌های اصلی پروژه کپی کنید.

### مرحله ۲: اجرای اسکریپت پچ
```bash
python apply_patch.py /path/to/your/bot/project
```

### مرحله ۳: تنظیم کلید API نشان
در فایل `.env` پروژه:
```
NESHAN_API_KEY=service.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

### مرحله ۴: نصب وابستگی‌ها
```bash
pip install weasyprint beautifulsoup4
```

### مرحله ۵: ری‌استارت ربات

## فایل‌های جدید

| فایل | توضیح |
|------|-------|
| `tax_geolocation_query.py` | هسته استعلام از tax.gov.ir (جدول استان‌ها + استخراج اطلاعات) |
| `geocode_and_query.py` | پایپ‌لاین تبدیل آدرس به مختصات با نشان + استعلام |
| `regional_value_pdf.py` | تولید PDF گزارش با WeasyPrint |
| `regional_value_handlers.py` | هندلرهای مکالمه ربات (FSM) |
| `apply_patch.py` | اسکریپت خودکار اعمال تغییرات |

## تغییرات خودکار روی فایل‌های موجود

- **states.py** — ۵ حالت FSM جدید
- **keyboards.py** — دکمه «🗺️ ارزش منطقه‌ای» در منو اصلی
- **config.py** — `NESHAN_API_KEY` + `REGIONAL_VALUE_FEE=200000`
- **handlers.py** — ایمپورت روتر + مسیریابی منو + هندلر پرداخت
- **env.example** — `NESHAN_API_KEY`
- **requirements.txt** — `weasyprint` + `beautifulsoup4`

## فلو کاربر
1. انتخاب از منوی اصلی: «🗺️ ارزش منطقه‌ای»
2. انتخاب استان از لیست ۳۱ استان
3. ورود آدرس دقیق با ذکر نام شهر
4. ورود متراژ عرصه (متر مربع)
5. انتخاب کاربری: مسکونی / تجاری / اداری
6. پرداخت ۲۰۰,۰۰۰ تومان از طریق کیف پول بله
7. دریافت فایل PDF گزارش

## نکات امنیتی
- `NESHAN_API_KEY` از متغیر محیطی خوانده می‌شود، **هرگز** هاردکد نشود
- کلید API نشان باید از نوع «سرویس‌ها»/Server باشد
